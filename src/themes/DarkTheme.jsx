import { theme } from 'antd';

const darkTheme =
  {
  algorithm: theme.darkAlgorithm,
  token: {
    colorBgBase: "#15202B",
    components: {
      Layout: {
        colorBgLayout: "#ffffff",
      }
    }
  },
};

export default darkTheme;
